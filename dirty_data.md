# 脏数据登记

本文件登记已核实或已定位的数据问题，供使用方规避。目前覆盖 `STOCK_DAILY` 与 `DAILY_BASIC` 两张表。每条都注明**判定依据**和
**是哪一方错**——没有依据的一律标「无法判定」，不臆测。

- 更新日期：2026-09-14

## 目录

- [STOCK_DAILY](#stock_daily)
- [DAILY_BASIC](#daily_basic)
- [ADJ_FACTOR](#adj_factor)
- [能否用于回测](#能否用于回测)

---

# STOCK_DAILY

核对范围：**2008-01-01 ~ 2016-09-11**（与通达信本地 `.day` 全量比对得出）；
2007 年及以前见「未覆盖区间」一节。

## 核对方法与仲裁判据

| 来源 | 覆盖区间 | 说明 |
|---|---|---|
| baostock | 全历史 | `STOCK_DAILY` 主源，含真实 `pre_close` |
| 同花顺 dump | 2016-09-12 起 | 无 `pre_close` 列 |
| 通达信 `.day` | 多数股票 2007-01-11 起 | 本地文件，含量额 |

两方分歧时用这两条**内部判据**仲裁，它们不依赖任何外部源：

1. **次日 `pre_close`**：交易所口径的次日前收必等于当日真实收盘（次日非除权日时成立）
2. **均价必在区间内**：`amount / volume` 按定义必须落在当日 `[low, high]` 内

## 一、已确认我方错误

### 002016.SZ 世荣兆业 / 2008-10-24

| 字段 | 库内 | 通达信 | 次日 `pre_close` | 判定 |
|---|---|---|---|---|
| close | 3.38 | **3.36** | **3.36** | 库内错 |
| low | 3.37 | **3.36** | — | 库内错（真实 close 3.36 低于库内 low 3.37，内部矛盾） |

**这是 2008–2016 区间内唯一一条确认我方有错的记录。** 注意它不是单字段错——`close`
和 `low` 同时偏高 0.01~0.02，只改 `close` 会造出 `close < low` 的非法行，须整行核对后
替换，故**尚未修复**。

## 二、已确认通达信错误（我方数据正确）

以下 7 行经内部判据证实库内数据正确，**使用方无需规避**，登记于此仅为说明比对差异来源。

| 代码 | 名称 | 日期 | 字段 | 判定依据 |
|---|---|---|---|---|
| 600652.SH | 退市游久 | 2009-09-02 | volume/amount | 通达信 volume 为库内 35 倍，其均价 0.077 远低于当日 `[5.42, 5.63]` |
| 000685.SZ | 中山公用 | 2015-06-09 | close/low/volume/amount | 次日前收 37.28 支持库内；通达信 volume 仅为库内 1/5 |
| 002457.SZ | 青龙管业 | 2015-06-09 | close/high/volume/amount | 次日前收 17.24 支持库内；通达信 volume 仅为库内 1/4 |
| 000725.SZ | 京东方A | 2015-05-28 | volume | 通达信 int32 溢出，见下 |
| 601988.SH | 中国银行 | 2015-06-09 | volume | 同上 |
| 601288.SH | 农业银行 | 2015-07-06 | volume | 同上 |
| 601988.SH | 中国银行 | 2015-07-08 | volume | 同上 |

**通达信 `.day` 的 `volume` 字段是 int32，上限 2,147,483,647。** 成交量超过此值时该
字段溢出，不可用于比对：

    000725.SZ 2015-05-28  库内 5,135,466,496 股   通达信 51,354,665（溢出）
    601988.SH 2015-07-08  库内 5,109,897,472 股   通达信 51,098,972（溢出）

两边 `amount` 完全一致，且库内均价落在当日区间内，确认库内正确。

## 三、无法判定（两方分歧，无仲裁依据）

### 3.1 2008-10-24 深市 22 只，volume 与 amount 同时偏低 1%~7%

库内量额同时低于通达信，**且两者比例基本相同**（`vol比` 0.9303~0.9899，
`amt比` 0.9301~0.9899），因此均价被保留，两方均价都落在当日区间内——
**均价判据对这批失效**。

| 代码 | 名称 | 库内 volume | 通达信 volume | 比值 |
|---|---|---|---|---|
| 000020.SZ | 深华发A | 310,833 | 314,833 | 0.9873 |
| 000034.SZ | 神州数码 | 2,144,322 | 2,166,722 | 0.9897 |
| 000036.SZ | 华联控股 | 6,174,537 | 6,240,637 | 0.9894 |
| 000065.SZ | 北方国际 | 1,022,004 | 1,073,653 | 0.9519 |
| 000505.SZ | 京粮控股 | 4,171,736 | 4,259,836 | 0.9793 |
| 000518.SZ | 四环生物 | 6,021,692 | 6,465,891 | 0.9313 |
| 000576.SZ | 甘化科工 | 2,925,265 | 2,971,965 | 0.9843 |
| 000586.SZ | 汇源通信 | 1,618,125 | 1,669,025 | 0.9695 |

同日同样情形的还有：`000595.SZ 新能股份`、`000628.SZ 高新发展`、`000702.SZ 正虹科技`、
`000720.SZ 新能泰山`、`000727.SZ 冠捷科技`、`000819.SZ 岳阳兴长`、`000930.SZ 中粮科技`、
`000955.SZ 欣龙控股`、`002007.SZ 华兰生物`、`002011.SZ 盾安环境`、`002012.SZ 凯恩股份`、
`002016.SZ 世荣兆业`、`002042.SZ 华孚时尚`、`002046.SZ 国机精工`。

**推测但未证实**：差额可能是大宗交易或盘后交易的计入口径不同。单日、单市场、
比例接近，像口径差异而非随机错误；但没有第三方可仲裁，故不下结论。

### 3.2 单字段价格分歧 2 行

| 代码 | 名称 | 日期 | 字段 | 库内 | 通达信 |
|---|---|---|---|---|---|
| 601111.SH | 中国国航 | 2013-05-31 | open | 5.30 | 5.48 |
| 600898.SH | *ST美讯 | 2013-06-19 | high | 4.70 | 4.71 |

`open` 与 `high` 没有内部仲裁判据（次日 `pre_close` 只能验证 `close`，均价判据只能
验证量额），两行其余字段均一致。需第三方行情源才能定性。

## 四、未覆盖区间

| 区间 | 交易行 | 占比 | 外部可验性 |
|---|---|---|---|
| 2016-09-12 起 | 10,367,615 | 60.8% | 同花顺全量比对完成 |
| 2007-01-11 ~ 2016-09-11 | 4,712,417 | 27.7% | 通达信全量比对完成（本文件） |
| **2007-01-11 以前** | **1,958,426** | **11.5%** | **无任何外部源** |

2007 年以前只能做内部自检。已知该区间存在：

- **均价越界 200 行**，集中在 11 只股票的 2000–2001 年，其中 `600837.SH 海通证券`
  (53)、`600633.SH 浙数文化` (50)、`600833.SH 第一医药` (42)、`600083.SH *ST博信` (39)
  四只占 184 行。均价/收盘比值紧密聚集在 1.04~1.05，是系统性偏高约 5%。
  **这四只同时是复权因子核对中缺陷最集中的股票**，其 2000–2001 年数据在多个维度上
  都不可靠，建议整体视为不可信。
- `600845.SH 宝信软件` 2001-05~06：价格整段滞后一条记录（成交量却对齐），
  须重排该段而非改单点。

另：2007 年与通达信比对有 822 行分歧（close 816 行），经次日 `pre_close` 仲裁
**817 行支持库内**，确认为通达信侧 2007 年数据问题，库内正确，不在本登记范围。

## 五、其它已知问题（非本次比对范围）

- **`600387.SH 退市海越`（2007-01-11 ~ 2025-07-11）、`000594.SZ 国恒退`
  （2007-01-11 ~ 2015-07-13）**：日线由 `-s lday` 补入，`pre_close` 全部为
  `LAG(close)` 合成值，非交易所除权参考价。详见 README「已知问题 > lday」。
- **同花顺 dump 2025-11-27 / 2025-12-01 两天**：量额缩放有误（实测 3,370 行），
  已用通达信与外部复核确认库内为准。比对时须排除这两天。
- 已修复项（`_KNOWN_DAILY_CLOSE_ANOMALIES` 19 条、`_KNOWN_DAILY_AMOUNT_ANOMALIES`
  58 条）见 `util/dbutil.py`，不在本登记内。

---

# DAILY_BASIC

与 `STOCK_DAILY` 不同，**`DAILY_BASIC` 大部分字段是派生的**（由 `STOCK_DAILY` 与
`CAPITAL_DETAIL` 计算得出），因此自检能力强，几乎不需要外部源。

**外部源情况：同花顺 dump 与通达信 `.day` 都只提供 OHLCV，不含本表任何字段。**

| 字段 | 可验证性 |
|---|---|
| `float_mv` / `total_mv` | 可自检：应等于 `close × 对应股本` |
| `turnover_rate` | 可自检：应等于 `volume / float_shares × 100` |
| `total_shares` / `float_shares` | 可自检：`float ≤ total` |
| `limit_up` / `limit_down` | 可自检：应等于 `pre_close × (1 ± 板块涨跌幅)`，且 `close` 须在区间内 |
| `is_limit_up` / `is_limit_down` | 可自检：与 `close` 和涨跌停价的关系 |
| `volume_ratio` | 部分可自检：须为正数 |
| **`pe` / `pb`** | **既无外部源，也无法自检** —— 来自 baostock，依赖财报口径，未核实 |

## 一、整表遗漏

**291 只退市股在 `DAILY_BASIC` 中完全没有记录，共缺 1,090,831 行**，而这些股票在
`STOCK_DAILY` 里是有日线的。根因与复权因子、`import_daily`、`lday` 的缺口同源：
取候选时 `is_delist` 默认 `False`，股票一旦退市就进不了任何候选集。

缺得最多的：`600200.SH 退市苏吴`(6,300)、`000851.SZ *ST高鸿`(6,264)、
`000627.SZ *ST天茂`(6,240)、`600190.SH 退市锦港`(6,193)、`000622.SZ 恒立退`(6,186)。

反向另有约 3.2 万行：北交所 `920xxx` 有 `DAILY_BASIC` 行但无日线（仅股本字段有值），
来自 `sync_basic` 拉取北交所名录时写入。

## 二、已确认错误

### 2.1 000850.SZ 华茂股份 2001-03-26 ~ 04-10（12 行）

`total_shares` 记为 18,500,000（1,850 万股），而 `float_shares` 为 65,000,000
—— **流通股本大于总股本**，并导致 `float_mv > total_mv`。

    2001-03-26  float_shares 65,000,000   total_shares 18,500,000
                float_mv 14.22 亿          total_mv 4.05 亿

README「已知问题 > tdx」已记录此事，但**日期写成了 2021-03-26，实际是 2001-03-26**
（本次核对发现，已一并更正）。

### 2.2 close 超出 `[limit_down, limit_up]`：1,927 行

错的是涨跌停价，不是收盘价。板块涨跌幅比例本身正确（主板中位 1.10、创业板与
科创板 1.20），越界集中在**涨跌幅限制不适用的特殊交易日**：

    002898.SZ 2026-06-26  前收 8.30  收盘 0.35  limit [7.47, 9.13]   退市整理期
    000004.SZ 2026-06-23  前收 2.76  收盘 0.31  limit [3.04, 2.48]   退市整理期

按年分布以 2006 年最多（684 行）。2020 年以后 51 行中，15 行为退市股、
36 行为 7 只在市股（多为新股上市首日等特殊情形）。**当前实现未区分这些情形，
一律按常规涨跌幅计算。**

## 三、存疑但未定性

### 3.1 turnover_rate 重算不符：206,797 行（1.29%）

`turnover_rate` 与 `volume / float_shares × 100` 不一致。但偏差温和——13 万行落在
0.95~1.05 倍之间，仅 1,309 行低于 0.5 倍、1,091 行高于 2 倍。按年以 2018–2023 最多。

**推测但未证实**：更像股本口径的时点差异（换手率计算时点的流通股本与当日
`float_shares` 不是同一版本），而非错值。未下结论。

### 3.2 601918.SH 新集能源 2010-12-20 ~ 2013-08-15（86 行）

`float_shares` 比 `total_shares` 多 **156 股**（1,850,387,031 vs 1,850,386,875）。
量级上是股本尾数差异而非口径错误，但严格讲仍违反 `float ≤ total`。

### 3.3 volume_ratio ≤ 0：510 行

按年分散，2015 年最多（226 行 / 157 只）。量比按定义应为正数，成因未查。

### 3.4 pb = 0：4,769 行（75 只股票）

集中在 2007（2,503 行）与 2006（1,068 行）。疑似缺值被当作 0 写入。
另有 154,900 行 `pb < 0`，**那是合法值**（净资产为负时市净率为负），不属异常。

## 四、已修复（本次）

**15 行派生市值使用了过期的 `close`。** 起因是修 `STOCK_DAILY.close` 时未同步重算
`DAILY_BASIC` 的派生字段——例如 `000595.SZ` 2008-10-24 的 `close` 已改为 6.75，
而 `float_mv / float_shares` 反推仍是 6.71。

已按 `fill_shares`（含 `fill_daily_basic_mv`）→ `fill_turnover` 的顺序重算
2003-11-17、2008-10-24、2013-05-20、2013-07-12、2013-08-09、2016-12-01、2017-10-24
七个交易日。复核：派生市值不一致 15 → 0，全库 `float_mv` 重算不符 3 → 0。

**教训：修原始字段后必须重算其派生字段。** `DAILY_BASIC` 的 `float_mv`/`total_mv`/
`turnover_rate` 均依赖 `STOCK_DAILY.close` 与 `volume`。

---

# ADJ_FACTOR

这张表不是原始数据，也不是单纯派生——它是 `ADJ_FACTOR_LOCAL` 事件链经
`ADJ_FACTOR_LOCAL_STATE` 的基准水位**稠密化**的结果。因此有三层独立的失效可能，
须分开核对：

| 层 | 查什么 | 工具 |
|---|---|---|
| 1 事件链 | `ADJ_FACTOR_LOCAL` 的比例对不对 | `tools/checks/adjust_invariant.py` |
| 2 稠密化 | `ADJ_FACTOR` 是否忠实展开了事件链 | 本节 |
| 3 可用性 | 用因子还原的收益率对不对 | 本节 |

## 一、层 2 稠密化：忠实，无独立错误

| 检查 | 违规 |
|---|---|
| `adjust_factor ≡ back_factor` | 0 |
| 因子为空或非正 | 0 |
| **稠密表 vs 事件链 ASOF 值**（逐行 1,761 万行） | **0** |

**稠密化本身零误差**——`ADJ_FACTOR` 没有独立于事件链的问题。这意味着修事件链即可
修好本表，不需要单独维护稠密表。

三处例外，均已定位且范围极小：

- **`fore_factor` 锚定不符 6,437 行，仅 3 只退市股**：`600087.SH 退市长油`（3,484 行）、
  `600296.SH S兰铝`（1,635 行）、`000765.SZ *ST华信`（1,318 行）。
  `fore_factor` 应等于 `back_factor / 该股最后一日 back_factor`，这三只不满足，
  疑为历史遗留的过期值。**重跑这三只的因子即可修复，尚未执行。**
- **有日线无因子行 11,133 行，仅 2 只**：`600767.SH 退市运盛`、`600652.SH 退市游久`
  —— 即配股价问题被排除计算的那两只（见 STOCK_DAILY 一节）。
- **有因子行无日线 2,630 行**：多为 `000594.SZ`（1,688 行）与 `600387.SH`（706 行）
  在 2007-01-11 之前的稠密行，其日线由 lday 补入而 lday 数据起点即 2007-01-11。

## 二、层 3 可用性：99.905% 完全一致

判据：**后复权收益率必须等于交易所口径收益率**

    (close[t] × back[t]) / (close[t-1] × back[t-1]) - 1  ==  close[t] / pre_close[t] - 1

这是回测实际经历的性质。可比 17,016,354 行：

| 偏差 | 行数 | 占比 |
|---|---|---|
| 完全一致（≤1e-6） | 17,000,169 | **99.905%** |
| ≤0.01% | 4,031 | |
| ≤0.1% | 10,180 | |
| ≤1% | 576 | |
| **>1%** | **1,398** | 0.008% |

偏差 >0.1% 的 1,974 行中，**1,457 行（74%）在 2007 年以前**（无外部源可验的盲区），
2007–2010 有 198 行，2011 年以后仅 319 行 / 209 只股票。

偏差最大的几条全部是同一类问题——**缩股与重组复牌**：

| 代码 | 日期 | 交易所收益率 | 复权收益率 |
|---|---|---|---|
| 600629.SH | 2007-10-26 | +10.98 | +25.62 |
| 000156.SZ | 2012-10-19 | +6.21 | +16.77 |
| 000622.SZ | 2013-02-08 | +3.73 | +13.20 |
| 000620.SZ | 2011-07-08 | +3.63 | +11.59 |
| 600381.SH | 2014-09-30 | +0.05 | +7.46 |

**根因是结构性能力缺口**：除权除息公式产生不了小于 1 的比例（缩股会让价格上涨），
而 gbbq 把缩股记在 `股本变化` 类，`local_xdr._EVENT_CATEGORIES` 不读该类
（读了会因字段双重语义算出荒谬因子）。要覆盖缩股需单独设计解析路径。

---

# 能否用于回测

## 结论

**2007 年以后可以用，但必须先处理幸存者偏差；2007 年以前不建议直接用。**

## 一、最危险的一条：幸存者偏差（DAILY_BASIC）

| | 股票数 | 有指标数据 | 无指标数据 |
|---|---|---|---|
| 在市 | 5,219 | 5,219 | 0 |
| **已退市（有日线）** | **312** | **21** | **291** |

**任何 JOIN `DAILY_BASIC` 的回测都会静默丢掉 291 只退市股**——而这些正是走向退市的
那批。按退市年份看，2020–2025 就占 181 只。这会系统性抬高回测收益，且**不会报错、
不会告警**，是三张表里最危险的问题。

`ADJ_FACTOR` 没有此问题（337 只退市股覆盖 310 只），`STOCK_DAILY` 也没有（312 只有日线）。

**规避方式**：需要 `DAILY_BASIC` 字段时用 `LEFT JOIN` 而非 `JOIN`，并显式处理 NULL；
或先补齐退市股的 `DAILY_BASIC`（根因是取候选时 `is_delist` 默认 `False`）。

## 二、按用途分类

| 用途 | 可用性 |
|---|---|
| **价格类策略**（动量、均线、突破） | **可用**。OHLC 在 2007 年后已逐行外部验证，不符率低于百万分之一 |
| **复权收益率**（绝大多数策略的基础） | **可用**。99.905% 与交易所口径完全一致 |
| **市值类**（市值因子、分组） | 可用，但须注意幸存者偏差；`float_mv`/`total_mv` 自检已零违规 |
| **换手率类** | 谨慎。1.29% 的行与重算值不符，成因未定 |
| **涨跌停判断** | 谨慎。`limit_up`/`limit_down` 在退市整理期等特殊日不适用常规涨跌幅，1,927 行 `close` 越界 |
| **估值类**（PE/PB） | **无法保证**。`pe`/`pb` 既无外部源也无法自检，从未核实 |
| **北交所** | **不可用**。344 只完全无日线 |

## 三、按区间分类

| 区间 | 交易行 | 建议 |
|---|---|---|
| 2016-09 起 | 10,367,615 | 可用，同花顺全量验过 |
| 2007-01 ~ 2016-09 | 4,712,417 | 可用，通达信全量验过 |
| **2007-01 以前** | **1,958,426** | **不建议**。无任何外部源；已知 11 只股票的 2000–2001 年数据在多维度不可靠；复权偏差 >0.1% 的 74% 集中于此 |

## 四、必须单独剔除的个股

- `600767.SH 退市运盛`、`600652.SH 退市游久`：无复权因子
- `600387.SH 退市海越`、`000594.SZ 国恒退`：`pre_close` 为合成值，复权因子只能按名义比例算且无从校验
- `600837.SH 海通证券`、`600633.SH 浙数文化`、`600833.SH 第一医药`、`600083.SH *ST博信`
  的 **2000–2001 年**：多维度数据不可靠
- 发生过**缩股或重组复牌**的个股（如 `000156.SZ`、`000620.SZ`、`600381.SH`、
  `000622.SZ`）：复权因子在该事件日错得很大，单日收益率可差数倍

## 复现方式

    # 与同花顺比对（2016-09-12 起）
    PYTHONPATH=. python tmp/audit_stock_daily.py

    # 与通达信比对（2007-01-11 ~ 2016-09-11），约 2 分钟
    PYTHONPATH=. python tmp/cmp_tdx.py
